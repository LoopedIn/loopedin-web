version: '3'

services:
  app:
    container_name: looped-in
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - '80:80'
    links:
      - mongo

  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017'